const mergeSort = (arr) => {
    if (arr.length === 1) {
        return arr
    }
    let mid = Math.floor(arr.length / 2)
    let arrayLeft = arr.slice(0, mid)
    let arrayRight = arr.slice(mid, arr.length)
    return temp2

}

console.log(mergeSort([1, 4, 3]))

