

function binarySearchTree(arr, val) {
    let length = arr.length;
    let value = (arr(Math.floor(length / 2)) === val) ? true : false;
    (length < 2 && arr[0] === val) ? true : false;
    if (value) {
        return
    }


}

binarySearchTree([1, 3, 5], 3)