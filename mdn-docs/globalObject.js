Infinity // it is the global object

NaN // it is also the property og global objects

// to check NaN value 
isNaN(Nan)// true
isNaN(Number.NaN)// true  // ES6

// but 
NaN === NaN // false  


undefined

var x,
(x === undefined) ? true : false;